[package]
name = "quantum_wager"
version = "0.1.0"
description = "Quantum Wager — minimal devnet prediction-market escrow (UNAUDITED)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "quantum_wager"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.31.1"
